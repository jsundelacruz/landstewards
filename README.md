# landstewards

```
landstewards_app/ - Flutter source code
static/ - The built web app ready for deployment
Makefile - used to easily build the app.
Dockerfile - The docker config file
fly.toml - The Fly.io config file
```
